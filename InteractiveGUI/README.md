Using the interactive compressor GUI
=======
In order to run the interactive GUI for compressor inlet operations, run the command listed in ```InstallModules.sh``` in order to install the necessary python modules.
Then, you can run the ```Interactive_CompressorGUI.py``` script to start the GUI program. Have fun with it!